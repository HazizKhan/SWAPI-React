import Search from "../Search";
import List from "../List";
import { Container } from "@mui/material";

const Main = () => {
  return (
    <Container>
      <div>
        <Search />
        <List />
      </div>
    </Container>
  );
};

export default Main;
